# Core dataset / imaging I/O
nibabel>=5.4.0
numpy>=2.0.0
matplotlib>=3.10.0
pillow>=11.0.0
scipy>=1.13.0

# Configuration & Data Handling
pyyaml>=6.0.0
pandas>=2.0.0

# Deep learning stack (CPU is sufficient for local dev/smoke tests;
# Colab installs the CUDA build automatically via its default index).
torch>=2.2.0

# Testing
pytest>=8.0.0

